<% if book.errors.any? %>
  <div class="alert alert-warning">
    <ul>
      error
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>